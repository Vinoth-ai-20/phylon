edition = "2021"
max_width = 100
use_small_heuristics = "Default"
newline_style = "Unix"

# Note: imports_granularity and group_imports are nightly-only features.
# They are intentionally omitted here to support the stable toolchain.
# When the project upgrades to nightly for the gpu crate, add:
#   imports_granularity = "Crate"
#   group_imports = "StdExternalCrate"
