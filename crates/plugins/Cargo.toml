[package]
name = "plugins"
version = "0.1.0"
edition = "2021"
description = "rhai scripting engine, plugin loader, and simulation API bindings."

[dependencies]
common    = { workspace = true }
thiserror = { workspace = true }
