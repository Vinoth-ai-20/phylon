[package]
name = "events"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { workspace = true }
crossbeam = { workspace = true }
genetics = { workspace = true }
organisms = { version = "0.1.0", path = "../organisms" }
serde = { workspace = true, features = ["derive"] }
