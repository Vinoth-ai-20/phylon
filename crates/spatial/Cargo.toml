[package]
name = "spatial"
version = "0.1.0"
edition = "2021"
description = "Spatial indexing structures for Phylon: uniform grid, quadtree, spatial hash."

[dependencies]
common    = { workspace = true }
bevy_ecs  = { workspace = true }
thiserror = { workspace = true }
